spring:
  application:
    name: config-service

  cloud:
    config:
      server:
        git:
          uri: https://github.com/KaranLalwani-dev/designforge-config-server.git
          username: KaranLalwani-dev
          password: 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
          default-label: main

server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: health,info
  health:
    config:
      enabled: false

---

spring:
  config:
    activate:
      on-profile: k8s

  cloud:
    discovery:
      enabled: false
    config.server.git:
      uri: https://github.com/KaranLalwani-dev/designforge-config-server.git
      username: ${GIT_USERNAME}
      password: ${GIT_PASSWORD}

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

